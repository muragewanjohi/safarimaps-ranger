import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.safarimapsranger.app',
  appName: 'SafariMaps Ranger',
  webDir: 'www.safarimaps.org'
};

export default config;
